/* 
PORT=3030
HOST=us-cdbr-east-05.cleardb.net
USER='bcb2adfad530a2'
PASSWORD='5e1e84ca'
PORTDB=3306
DATABASE=heroku_b94d852201155b2 
*/


/* 
Si utilizamos la DB local, se deben cambiar los ID de todas las consultas ya que HEROKU utiliza 
otro sistema de numeracion

PORT=3030
HOST=localhost
USER='root'
PASSWORD='1234'
PORTDB=3306
DATABASE=dbtp
*/



/* 
-- Creamos la DB -- 
create database dbtp;
-- Seleccionamos la DB --
use dbtp;
-- Creamos tablas: --

create table planes(
	id int unsigned not null auto_increment,
    plan varchar(50) not null,
    primary key(id)
);

create table formulario(
	id int unsigned not null auto_increment,
    firstName varchar(80) not null,
    lastName varchar(80) not null,
    username varchar(80),
    email varchar(100) not null,
    address varchar(150) not null,
	address2 varchar(150),
    country varchar(100) not null,
    state varchar(100) not null,
    zip int unsigned not null,
	plan int unsigned not null,
    message varchar(255) not null,
    dataStorage varchar(10) not null,
    beContacted varchar(10),
    metOn varchar(15) not null,
    primary key(id),
    foreign key (plan) references planes(id)
);
create table newsletter(
	id int unsigned not null auto_increment,
    email varchar(100) not null,
    plan int unsigned not null,
    primary key(id),
    foreign key (plan) references planes(id)
);

create table registro(
id int unsigned not null auto_increment,
firstName varchar(100) not null,
email varchar(100) not null,
pass varchar(100) not null,
plan int unsigned not null,
primary key(id),
foreign key (plan) references planes(id)
);


INSERT INTO PLANES VALUES ( NULL, "FREE"); 
INSERT INTO PLANES VALUES ( NULL, "BASIC"); 
INSERT INTO PLANES VALUES ( NULL, "PREMIUM"); 
INSERT INTO PLANES VALUES ( NULL, "PREMIUM D+"); 
INSERT INTO PLANES VALUES ( NULL, "CANCEL"); 

*/